[project]
name = "julia-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rlopc4ds", email = "rlopc4ds@gmail.com" }
]
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastapi>=0.115.8",
]

[project.scripts]
julia-api = "julia_api:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
